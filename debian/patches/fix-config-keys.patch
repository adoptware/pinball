Description: Fix error on configuring of keys.
Author: Innocent De Marchi <tangram.peces@gmail.com>
Bug-Debian: http://bugs.debian.org/858627
Last-Update: 2018-03-16

--- pinball-0.3.1.orig/src/Pinball.cpp	2003-11-20 17:46:17.000000000 +0100
+++ pinball-0.3.1/scr/Pinball.cpp	2018-03-16 19:27:59.899754718 +0100
@@ -151,13 +151,32 @@
     } else {
       key = string("unknown");
     }
+
+	if (name.length() + key.length() > 15){  
+	    std::size_t found = name.find("leftflip");
+	if (found!=std::string::npos)
+	    name = "LF";
+	found = name.find("rightflip");
+	if (found!=std::string::npos)
+	    name = "RF";
+	found = name.find("leftnudge");
+	if (found!=std::string::npos)
+	     name = "LN";
+	found = name.find("rightnudge");
+	if (found!=std::string::npos)
+	     name = "RN";
+	found = name.find("bottomnudge");
+	if (found!=std::string::npos)
+	     name = "BN";
+	}
+
     name = name + ":";
-    while (name.size() < 12) {
+    /*while (name.size() < 12) {
       name = name + " ";
     }
     while (key.size() < 12) {
       key = " " + key;
-    }
+    }*/
     stm << name << key << '\0';
     string text = stm.str();
     return text.c_str();
