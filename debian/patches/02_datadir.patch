From: Innocent De Marchi <tangram.peces@gmail.com>
Date: Fri, 31 Jul 2020 12:18:31 +0200
Subject: Changes on data dir.

Last-Update: 2018-01-21
---
 base/Config.cpp      |  7 -------
 src/LoaderModule.cpp | 21 +++++++++++++++++++++
 src/LoaderModule.h   |  3 +++
 src/Table.cpp        |  2 +-
 4 files changed, 25 insertions(+), 8 deletions(-)

diff --git a/base/Config.cpp b/base/Config.cpp
index a494ef9..11ed53c 100755
--- a/base/Config.cpp
+++ b/base/Config.cpp
@@ -358,13 +358,6 @@ void Config::loadArgs(int & argc, char *argv[]) {
       }
       EM_COUT("Using " << m_iBpp << " bpp", 1);
       REMOVEARG(a, argc, argv);
-    } else if (strcmp(argv[a], "-data") == 0) {
-      if (argc > a) {
-	EM_COUT("Using datapath: " << argv[a+1], 1);
-	this->setDataDir(argv[a+1]);
-	REMOVEARG(a, argc, argv);
-      }
-      REMOVEARG(a, argc, argv);
     } else if (strcmp(argv[a], "-nosound") == 0) {
       this->setSound(0);
       this->setMusic(0);
diff --git a/src/LoaderModule.cpp b/src/LoaderModule.cpp
index 149df09..50903b4 100755
--- a/src/LoaderModule.cpp
+++ b/src/LoaderModule.cpp
@@ -61,6 +61,23 @@ LoaderModule* LoaderModule::getInstance()
 }
 //-----------------------------------------------------------------------------
 
+void LoaderModule::SanitizePath(std::string&str )
+{
+  // String to remove.
+  std::string sub = "../";
+
+  typedef std::string::size_type st;
+  typedef std::string::iterator it;
+  st npos = std::string::npos;
+  st pos = 0;
+    
+  while((pos = str.find(sub)) != npos) {
+    it first = str.begin() + pos;
+    it last = first + sub.size();
+    str.erase(first, last);
+  }
+}
+
 /// is it possible to do something 
 /// like in java.lang.reflexion instead of protypes ?
 Behavior* LoaderModule::readLibStatic(string & filename) 
@@ -82,6 +99,10 @@ Behavior* LoaderModule::readLibDynamic(string & filename)
 {
   EM_COUT("+ LoaderModule::readLibDynamic",0);
   Behavior * beh =0;
+
+  // Disallow directory traversal
+  SanitizePath( filename );
+
 #ifndef RZR_LIBSTATIC
   lt_dlhandle handle = lt_dlopen(filename.c_str());
   if (handle == NULL) {
diff --git a/src/LoaderModule.h b/src/LoaderModule.h
index 89e0ceb..222a691 100755
--- a/src/LoaderModule.h
+++ b/src/LoaderModule.h
@@ -40,6 +40,9 @@ public:
   /// portable dyn lib loading
   Behavior* readLibDynamic(string & filename);
 
+private:
+  void SanitizePath(std::string&str );
+
 protected:
   ///
   map<char* const, Behavior* , ltstr> m_hMods;
diff --git a/src/Table.cpp b/src/Table.cpp
index 1976e2d..e98d573 100755
--- a/src/Table.cpp
+++ b/src/Table.cpp
@@ -140,7 +140,7 @@ int Table::loadLevel(Engine * engine, const char * subdir) {
   Loader::getInstance()->clearSignalVariable();
   if (Loader::getInstance()->loadFile(filename.c_str(), engine) != 0) {
     cerr << "Error loading level: " << m_sTableName << endl;
-    cerr << "Try reinstalling the game or use the -data switch to specify the data directory"
+    cerr << "Try reinstalling the game"
          << endl;
     this->clear(engine);
     return -1;
